{% extends 'base.html.twig' %}

{% block body %}
    <h1>ImagenInformacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagenInformacion.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('imageninformacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('imageninformacion_edit', { 'id': imagenInformacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
