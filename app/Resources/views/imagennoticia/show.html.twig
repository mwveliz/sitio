{% extends 'base.html.twig' %}

{% block body %}
    <h1>ImagenNoticia</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagenNoticium.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('imagennoticia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('imagennoticia_edit', { 'id': imagenNoticium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
