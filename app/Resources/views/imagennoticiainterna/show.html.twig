{% extends 'base.html.twig' %}

{% block body %}
    <h1>ImagenNoticiainterna</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagenNoticiainterna.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('imagennoticiainterna_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('imagennoticiainterna_edit', { 'id': imagenNoticiainterna.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
