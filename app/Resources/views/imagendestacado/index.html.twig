{% extends 'base.html.twig' %}

{% block body %}
    <h1>ImagenDestacado list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for imagenDestacado in imagenDestacados %}
            <tr>
                <td><a href="{{ path('imagendestacado_show', { 'id': imagenDestacado.id }) }}">{{ imagenDestacado.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('imagendestacado_show', { 'id': imagenDestacado.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('imagendestacado_edit', { 'id': imagenDestacado.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('imagendestacado_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
